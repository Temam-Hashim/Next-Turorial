export const clientOnlyFunction = ()=>{
    console.log(`
               This is client only functionality,
               only uses window object,
               uses local storage`);

               return "clientOnly functionality";
}